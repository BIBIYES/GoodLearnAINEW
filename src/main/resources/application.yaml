spring:
  web:
    resources:
      static-locations: classpath:/static/
  application:
    name: GoodLearnAI
  datasource:
    url: jdbc:mysql://112.124.40.32:3306/good_learn_ai_NEW_local?useSSL=false&serverTimezone=UTC
    username: root
    password: zzx
    driver-class-name: com.mysql.cj.jdbc.Driver
  devtools:
    restart:
      enabled: true
      # 自定义监控的路径，这里只监控 src/main/java 目录
      additional-paths: src/main/java
      # 排除不必要的路径，防止静态资源文件等变化触发重启
      exclude: static/**,public/**,templates/**,log/**
  ai:
    openai:
      api-key: sk-f8bdafb2d21b4639b7139876ddce109a
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
    mcp:
      client:
        stdio:
          servers-configuration: ./classpath:mcp-servers.json
  mvc:
    servlet:
      path: /api
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
      file-size-threshold: 2MB
  data:
    redis:
      host: 112.124.40.32
      port: 6379
      timeout: 5000
      password: zxcvg123

server:
  port: 8080

# 文件上传配置
file:
  upload:
    # 文件上传基本路径
    base-path: C:/files/
    # 文件访问URL前缀
    access-url: /files/

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:mapper/*.xml

jwt:
  secret: mouse-mouse
  expirationMs: 86400000

logging:
  level:
    root: info
    com.example.goodlearnai: debug
